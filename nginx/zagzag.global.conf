# ==============================================
# HTTP 80端口配置：支持直接访问，无强制跳转
# ==============================================
server {
    listen 80;
    listen [::]:80;
    server_name *.zagzag.qzz.io;

    # ------------------------------
    # 根路径：Xboard面板（端口7001）
    # ------------------------------
    location / {
        proxy_pass http://127.0.0.1:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;

        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_cache off;
        proxy_buffering off;
    }

    # ------------------------------
    # 子路径：订阅转换服务（端口3000）
    # ------------------------------
    location ^~ /fetcher/ {
        rewrite ^/fetcher/(.*)$ /$1 break;

        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_cache off;
        proxy_buffering off;
    }
}

# ==============================================
# HTTPS 443端口配置：无IP拦截
# ==============================================
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name *.zagzag.qzz.io;

    # SSL证书配置
    ssl_certificate /root/.lego/certificates/_.zagzag.qzz.io.crt;
    ssl_certificate_key /root/.lego/certificates/_.zagzag.qzz.io.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # ------------------------------
    # 根路径：Xboard面板（端口7001）
    # ------------------------------
    location / {
        proxy_pass http://127.0.0.1:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;

        # WebSocket支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_cache off;
        proxy_buffering off;
    }

    # ------------------------------
    # 子路径：订阅转换服务（端口3000）
    # ------------------------------
    location ^~ /fetcher/ {
        rewrite ^/fetcher/(.*)$ /$1 break;

        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_cache off;
        proxy_buffering off;
    }
}

# ==============================================
# 兜底配置：IP直连/其他域名访问
# ==============================================
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    server_name _;

    ssl_certificate /root/.lego/certificates/_.zagzag.qzz.io.crt;
    ssl_certificate_key /root/.lego/certificates/_.zagzag.qzz.io.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # ------------------------------
    # 子路径：订阅转换服务（端口3000）
    # ------------------------------
    location ^~ /fetcher/ {
        rewrite ^/fetcher/(.*)$ /$1 break;

        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Port $server_port;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_cache off;
        proxy_buffering off;
    }

    # ------------------------------
    # 根路径：Xboard面板（端口7001）
    # ------------------------------
    location / {
        proxy_pass http://127.0.0.1:7001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}